---
- name: Ubuntu Install Nginx
- hosts: all
  become: yes
  apt:
    name=nginx
    state=latest
  when:
    ansible_os_family == "Ubuntu"
  notify:
    - web systemd

- name: web systemd
  systemd:
    name: nginx
    enabled: yes
    state: started